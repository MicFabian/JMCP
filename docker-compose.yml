version: '3.9'
services:
  mcp:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "9090:9090"
    environment:
      - JAVA_TOOL_OPTIONS=--enable-native-access=ALL-UNNAMED -XX:MaxRAMPercentage=80.0
    volumes:
      - ./data:/app/data
